#include <iostream>
#include <string>
#include <vector>

using namespace std;
#define rep(i,n) for(int i = 0; i < (n); ++i)

int
main(int argc, char* argv[]) {
	int a,b,c;
	string s;

	cin >> a;
	cin >> b >>  c;
	cin >> s;

	cout << a + b + c << " " << s << endl;
	
	return 0;
}
